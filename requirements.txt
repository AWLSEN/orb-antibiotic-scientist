# orb-antibiotic-scientist -- Python dependencies
# Add new deps alongside the commit that introduces them so requirements.txt
# stays aligned with commit history.

# Layers 1-4 (structure, hygiene, drug-likeness, SA Score)
rdkit>=2024.3.5
pyyaml>=6.0

# Agent harness (Claude Agent SDK)
claude-agent-sdk>=0.1.0

# Testing
pytest>=8.0
